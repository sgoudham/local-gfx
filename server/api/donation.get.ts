type Donation = {
  // TODO: fields that we care about
}

export default defineEventHandler(() => {
  const apiKey = process.env.JUSTGIVING_API_KEY
  // TODO: fetch call to justgiving 
  // return typed object 
});
